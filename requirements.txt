# Core dependencies
ollama
pyttsx3
SpeechRecognition
python-dotenv
PyQt5
pywhatkit
pyaudio

# Platform-specific dependencies
pyobjc-core; sys_platform == 'darwin'
pyobjc-framework-Cocoa; sys_platform == 'darwin'
pywin32; sys_platform == 'win32'
pycaw; sys_platform == 'win32'
comtypes; sys_platform == 'win32'

# AI Game Playing Dependencies
opencv-python>=4.8.0
pyautogui>=0.9.54
keyboard>=0.13.5
mouse>=0.7.1
Pillow>=10.0.0
numpy>=1.24.0

# System Control Dependencies
psutil>=5.9.0
pygetwindow>=0.0.9; sys_platform == 'win32'
wmi>=1.5.1; sys_platform == 'win32'

# NPU Acceleration (Optional - for Omen PC optimization)
# Intel NPU support via OpenVINO
openvino>=2024.0.0; sys_platform == 'win32'
openvino-dev>=2024.0.0; sys_platform == 'win32'

# ONNX Runtime with DirectML (Windows GPU/NPU)
onnxruntime-directml>=1.17.0; sys_platform == 'win32'

# AMD NPU support (Ryzen AI)
# onnxruntime-vitisai; sys_platform == 'win32'  # Uncomment if you have AMD Ryzen AI

# Optional: PyTorch with CUDA (if NVIDIA GPU available)
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# Optional: Advanced AI for game playing
# tensorflow>=2.13.0  # For deep learning models
# ultralytics>=8.0.0  # For YOLO object detection
